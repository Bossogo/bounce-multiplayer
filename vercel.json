{
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/ws", "destination": "/api/ws" }],
  "functions": {
    "api/ws.ts": {
      "maxDuration": 300
    }
  }
}
